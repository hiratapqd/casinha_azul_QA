<%- include('partials/header') %>
<link rel="stylesheet" href="/styles/styles.css">
<main class="hero">
    <h1>Cadastro de Assistido</h1>
    <form action="/assistido/novo" method="POST" class="form-container">
    <div class="form-row">
        <div class="form-group">
            <label for="cpf_assistido">CPF:</label>
            <input type="text" id="cpf_assistido" name="cpf_assistido" placeholder="000.000.000-00" required>
        </div>
        <div class="form-group">
            <label for="nome_assistido">Nome Completo:</label>
            <input type="text" id="nome_assistido" name="nome_assistido" required>
        </div>
    <br>
    </div><br>
    <div class="form-row">
        <div class="form-group">
            <label for="sexo_assistido">sexo:</label>
            <input type="string" id="sexo_assistido" name="sexo_assistido">
        </div><br>
        <div class="form-group">
            <label for="data_nascimento_assistido">Data de Nascimento:</label>
            <input type="date" id="data_nascimento_assistido" name="data_nascimento_assistido">
        </div><br>
        <div class="form-group">
            <label for="religiao_assistido">Religião:</label>
            <input type="text" id="religiao_assistido" name="religiao_assistido">
        </div><br>
    </div><br>
    <div class="form-row">
        <div class="form-group">
            <label for="cidade_assistido">Cidade:</label>
            <input type="text" id="cidade_assistido" name="cidade_assistido">
        </div><br>
        <div class="form-group">
            <label for="uf_assistido">UF:</label>
            <input type="text" id="uf_assistido" name="uf_assistido">
        </div><br>
    </div><br>
    <div class="form-row">
        <div class="form-group">
            <label for="email_assistido">E-mail:</label>
            <input type="email" id="email_assistido" name="email_assistido">
        </div><br>
        <div class="form-group">
            <label for="telefone_assistido">Telefone/WhatsApp:</label>
            <input type="tel" id="telefone_assistido" name="telefone_assistido">
        </div><br>
    </div><br>
        <button type="submit" class="btn-atender btn-success">Finalizar Cadastro do assistido</button>
    </form>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>


<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script>
    const formAssistido = document.querySelector('form');

    formAssistido.addEventListener('submit', async (e) => {
        e.preventDefault(); // Impede o recarregamento da página

        const formData = new FormData(formAssistido);
        const params = new URLSearchParams(formData);

        try {
            const resposta = await fetch('/assistido/novo', {
                method: 'POST',
                body: params
            });

            const resultado = await resposta.json();

            if (resultado.status === 'existente') {
                // Alerta informando que o CPF já está na base
                Swal.fire({
                    title: 'CPF já cadastrado!',
                    html: `O assistido <b>${resultado.nome}</b> já possui cadastro na Casinha Azul.<br><br>Não é necessário cadastrar novamente.`,
                    icon: 'info',
                    confirmButtonText: 'Entendido',
                    confirmButtonColor: '#007bff'
                });
            } else if (resultado.status === 'sucesso') {
                // Alerta de sucesso total
                Swal.fire({
                    title: '✅ Sucesso!',
                    text: 'Assistido cadastrado com sucesso.',
                    icon: 'success',
                    showCancelButton: true,
                    confirmButtonText: 'Novo Cadastro',
                    cancelButtonText: 'Ir para o Início'
                }).then((result) => {
                    if (result.isConfirmed) {
                        window.location.reload();
                    } else {
                        window.location.href = '/';
                    }
                });
            } else {
                Swal.fire('Erro!', 'Ocorreu um erro ao salvar o assistido.', 'error');
            }
        } catch (error) {
            Swal.fire('Erro!', 'Não foi possível conectar ao servidor.', 'error');
        }
    });
</script>
</main>
<%- include('partials/footer') %>